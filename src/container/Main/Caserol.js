import React from 'react'

const Caserol = (props) => {
    return (
        <div style={{width: '100%'}}>
            <img src={props.img} alt='foto' style={{width: '100%', height: '100%'}} />
        </div>
    );
}


export default Caserol